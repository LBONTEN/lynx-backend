version: '3.4'

services:
  identifier:
    environment:
      VIRTUAL_HOST: "lynx.ai-data-store.s.redhost.be"
      LETSENCRYPT_HOST: "lynx.ai-data-store.s.redhost.be"
      LETSENCRYPT_EMAIL: "aad.versteden@redpencil.io"
    networks:
      - default
      - proxy
networks:
  proxy:
    external:
      name: letsencrypt_default

